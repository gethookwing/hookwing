/**
 * {{integrationName}} webhook handler
 * Generated by @hookwing/agent-skills
 */

import { create{{integrationName}}Handler } from '@hookwing/agent-skills/{{integrationName}}';

const handler = create{{integrationName}}Handler({
  signingSecret: process.env.{{integrationNameUpperCase}}_WEBHOOK_SECRET!,
});

export default handler;

// Handle webhook events
export async function handleWebhook(payload: string, signatureHeader: string) {
  const event = handler.verify(payload, signatureHeader);

  await handler.handle(event, {
    // Add your event handlers here
  });
}
