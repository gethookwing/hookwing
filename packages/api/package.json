{
  "name": "@hookwing/api",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@hookwing/shared": "*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "vitest": "^3.0.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.93.0"
  }
}
