# Hookwing Repo Cleanup Report
**Generated:** 2026-03-04  
**Branch at time of audit:** `feature/website-phase2`  
**Task:** PROD-19 — Repo cleanup assessment (no deletions made)

---

## 1. Branch Assessment

### Branches Found
```
  feature/blog-redesign           (LOCAL ONLY — not pushed)
  feature/tina-cms-cloudflare     (local + remote)
* feature/website-phase2          (local + remote) ← current
  main                            (local + remote)
```

### Verdict

| Branch | Action | Reasoning |
|---|---|---|
| `main` | ✅ **Keep** | Root branch, single init commit |
| `feature/website-phase2` | ✅ **Keep** | Active development, 1 commit + many uncommitted changes |
| `feature/blog-redesign` | ⚠️ **Flag for Fabien** | See note below |
| `feature/tina-cms-cloudflare` | 🗑️ **Delete (pending confirm)** | See note below |

#### `feature/blog-redesign` — LOCAL ONLY, 16 commits ahead of main
This branch is not pushed to remote. It includes a stash entry (`stash@{0}: On feature/blog-redesign: blog-redesign WIP`). The commits trace back through TinaCMS → blog redesign work. It appears to pre-date the current content pipeline approach (markdown files in `content/`). 

**⚠️ FLAG FOR FABIEN:** Is this branch safe to delete? The stash contains WIP work. If the blog redesign (design system v2, CSS rewrite) is no longer needed, this can be deleted. If any assets or patterns from it should be carried forward, they should be extracted first.

#### `feature/tina-cms-cloudflare` — Remote branch, 15 commits ahead of main
This branch represents the TinaCMS + Cloudflare Pages CMS exploration. The website has since moved to a simpler markdown file approach (`content/blog/`, `content/authors/`). TinaCMS infrastructure appears to have been abandoned. Commits include scaffolding, CMS workflow docs, and blog generation pipelines — all now superseded.

**Recommendation: Delete remote and local.** But confirm with Fabien first — if any of the deploy scripts or content pipeline logic from this branch should be preserved, extract before deleting.

---

## 2. Uncommitted Changes (feature/website-phase2)

This is important context — there's significant uncommitted work on the current branch.

### Deleted (staged for commit — design iteration files)
These were deleted from disk but not yet committed:
```
website/draft-hw-001/index.html
website/v1-a/ through website/v11-plans/index.html  (40+ design iteration files)
```
**Action needed:** Commit these deletions. They are design A/B iteration pages that are no longer needed.

### Untracked new files (not yet committed)
```
website/assets/blog/generated/
website/assets/blog/illustrations/
website/assets/team/
website/categories/
website/changelog/
website/content/           ← all blog content + authors
website/playground/
website/privacy/
website/rss.xml
website/search-index.json
website/signin/
website/status/
website/tags/
website/terms/
website/webhook-idempotency-checklist/
website/webhook-monitoring-observability/
website/webhook-retry-best-practices/
website/webhooks-for-ai-agents/
```
**Action needed:** Review and commit these when ready. Most are correct WIP pages aligned with the expected structure.

---

## 3. File Audit — Files to Remove

### 🗑️ Recommend Removing (internal working docs in website/)
These should never be deployed as part of the website:

| File | Reason |
|---|---|
| `website/AUDIT-REPORT.md` | Internal audit doc, not website content |
| `website/FIXES.md` | Internal fix log |
| `website/LINK-AUDIT.md` | Internal link audit |
| `website/SECURITY-AUDIT.md` | Internal security audit |

These were generated by Claude during earlier reviews and committed to the repo. They will be served by Cloudflare Pages unless removed.

### ⚠️ Flag for Fabien — Uncertain

| File/Dir | Question |
|---|---|
| `website/deploy-design-lab.sh` | Design lab deploy script at website root. Should this live in `website/scripts/` instead? Or is it obsolete now that phase 2 is active? |
| `design/` (at repo root) | Logos (`logo-01-flightpath-h.svg` etc.) and design tokens (sky-ops, radar-dark, etc.). Not in expected structure. Are these source assets for the design system? Should they be under `website/assets/` or kept as a separate design reference folder at root? |
| `website/blog/` | Contains rendered blog post HTML (`webhook-retry-best-practices/index.html`, `webhooks-for-ai-agents/index.html`). The same articles also exist at `website/webhook-retry-best-practices/` and `website/webhooks-for-ai-agents/` (root level). **URL duplication issue** — which is the canonical path? `/blog/article-slug/` or `/article-slug/`? |
| `website/rss.xml` | Generated file. Should this be committed to the repo or generated at build time? |
| `website/search-index.json` | Generated file. Same question — committed or build artifact? |
| `app/` | Empty placeholder directory. Safe to leave, but adds noise. |
| `agent.md` (repo root) | Claude Code instruction file. Not website content, but useful for dev context. Recommend keeping at root. |

### ✅ Leave As-Is (build artifacts — gitignored)
These exist on disk but are already excluded by `.gitignore`:
- `website-build/` — correctly gitignored via `.wrangler/` + explicit `website-build/` rule
- `website/.wrangler/` — covered by `.wrangler/` gitignore rule
- `.wrangler/` at root — covered

---

## 4. .gitignore Audit

### Current .gitignore
```gitignore
# Node / builds
node_modules/
dist/
.build/
coverage/

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12

# Terraform local state/artifacts
**/.terraform/
*.tfstate
*.tfstate.*
crash.log

# Git internals accidentally copied
**/.git/
**/.git.nested.backup/

# Tooling caches/state
.wrangler/
website-build/

# Logs / OS
*.log
.DS_Store
.vscode/
.idea/
```

### Missing Entries (should be added)

| Entry | Why |
|---|---|
| `.deploy-blog` | Mentioned as deploy artifact — not currently excluded |
| `.preview-dist` | Preview build artifact — not currently excluded |
| `secrets` | A file or dir named `secrets` — not excluded |
| `*.pages.dev` | ⚠️ **FLAG:** This is a URL pattern, not a file pattern. Unclear what this should map to in gitignore. Does it refer to files named `*.pages.dev`? Or is this a config artifact from Cloudflare Pages? Clarify what this should exclude. |

---

## 5. Current vs Desired Repo Structure

### Current (as committed on feature/website-phase2 + untracked)
```
hookwing/
├── .github/workflows/deploy.yml
├── .gitignore
├── .wrangler/                    ← gitignored
├── CLAUDE.md                     ← AI instructions (keep)
├── agent.md                      ← AI instructions (keep or move)
├── CLEANUP-REPORT.md             ← this file
├── api/                          ← Cloudflare Workers API ✅
│   ├── src/
│   ├── migrations/
│   ├── test/
│   ├── node_modules/             ← gitignored ✅
│   └── wrangler.jsonc
├── app/                          ← empty placeholder
├── design/                       ← logo SVGs + token files (uncertain location)
│   ├── logo/
│   └── tokens/
├── website/
│   ├── AUDIT-REPORT.md           ← 🗑️ remove
│   ├── FIXES.md                  ← 🗑️ remove
│   ├── LINK-AUDIT.md             ← 🗑️ remove
│   ├── SECURITY-AUDIT.md         ← 🗑️ remove
│   ├── README.md                 ← keep
│   ├── deploy-design-lab.sh      ← ⚠️ uncertain
│   ├── favicon.svg               ✅
│   ├── index.html                ✅
│   ├── rss.xml                   ← ⚠️ generated? or committed?
│   ├── search-index.json         ← ⚠️ generated? or committed?
│   ├── api/pricing/index.json    ✅
│   ├── assets/                   ✅
│   │   ├── blog/
│   │   ├── logos/
│   │   ├── og/
│   │   └── team/
│   ├── blog/                     ← ⚠️ URL duplication (see above)
│   │   ├── webhook-retry-best-practices/index.html
│   │   └── webhooks-for-ai-agents/index.html
│   ├── categories/               ← new, not in expected structure
│   ├── changelog/index.html      ✅ (WIP)
│   ├── content/                  ✅
│   │   ├── blog/ (markdown sources)
│   │   └── authors/
│   ├── getting-started/index.html ✅
│   ├── playground/index.html     ✅ (WIP)
│   ├── pricing/index.html        ✅
│   ├── privacy/index.html        ✅ (WIP)
│   ├── scripts/deploy-dev.sh     ✅
│   ├── signin/index.html         ✅ (WIP)
│   ├── status/index.html         ✅ (WIP)
│   ├── tags/                     ← new, not in expected structure
│   ├── terms/index.html          ✅ (WIP)
│   ├── webhook-idempotency-checklist/ ← ⚠️ root-level article, URL conflict with /blog/?
│   ├── webhook-monitoring-observability/ ← ⚠️ same
│   ├── webhook-retry-best-practices/   ← ⚠️ same (duplicate of blog/)
│   ├── webhooks-for-ai-agents/         ← ⚠️ same
│   └── why-hookwing/index.html   ✅
└── website-build/                ← gitignored ✅
```

### Desired (per spec)
```
hookwing/
├── api/                          ✅
├── website/
│   ├── index.html
│   ├── favicon.svg
│   ├── pricing/index.html
│   ├── why-hookwing/index.html
│   ├── getting-started/index.html
│   ├── playground/index.html     (WIP)
│   ├── status/index.html         (WIP)
│   ├── signin/index.html         (WIP)
│   ├── privacy/index.html        (WIP)
│   ├── terms/index.html          (WIP)
│   ├── changelog/index.html      (WIP)
│   ├── docs/                     (Docs pages — MISSING)
│   ├── api/pricing/index.json
│   ├── content/blog/             ✅
│   ├── content/authors/          ✅
│   ├── assets/                   ✅
│   └── scripts/                  ✅
```

### Notable Gaps vs Spec
- **`docs/` is missing** — expected in spec but no `website/docs/` directory exists yet
- **`categories/` and `tags/`** — not in spec but present; likely added as blog infrastructure. Fine to keep if intentional.
- **Root-level article directories** — (`webhook-retry-best-practices/`, `webhooks-for-ai-agents/`, `webhook-idempotency-checklist/`, `webhook-monitoring-observability/`) not in spec and conflict with `blog/` subdirectory. URL strategy needs decision.

---

## 6. Summary Checklist

### Safe to action immediately (no risk)
- [ ] Add missing `.gitignore` entries: `.deploy-blog`, `.preview-dist`, `secrets`
- [ ] Remove internal audit docs: `website/AUDIT-REPORT.md`, `website/FIXES.md`, `website/LINK-AUDIT.md`, `website/SECURITY-AUDIT.md`
- [ ] Commit the deleted design iteration files (v1-v11) on `feature/website-phase2`

### Needs Fabien's decision
- [ ] **Branches:** Delete `feature/tina-cms-cloudflare`? Delete `feature/blog-redesign` (+ stash)?
- [ ] **URL structure:** Are blog articles at `/blog/slug/` or `/slug/`? Remove duplicates once decided.
- [ ] **`design/` folder:** Keep at repo root or move/merge into `website/assets/`?
- [ ] **`website/deploy-design-lab.sh`:** Delete or move to `website/scripts/`?
- [ ] **`rss.xml` and `search-index.json`:** Committed intentionally or generated artifacts (gitignore)?
- [ ] **`*.pages.dev` gitignore pattern:** What does this actually need to exclude?
- [ ] **`docs/` directory:** Missing from spec — when is this being built?

---

*Report only — no files modified, no deletions made.*
